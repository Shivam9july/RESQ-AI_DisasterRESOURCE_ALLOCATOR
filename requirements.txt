# ---- Resq backend ----
Django>=4.2,<5.3
djangorestframework>=3.14,<4.0
django-cors-headers>=4.3,<5.0
requests>=2.31
python-dotenv>=1.0

# Production server
gunicorn>=21.2,<23
whitenoise>=6.6,<7

# PostgreSQL driver (Render managed Postgres)
psycopg2-binary>=2.9,<3

# Optional: for routing_optimization (DBSCAN, etc.) – uncomment if you use it from this repo
# numpy>=1.26
# scipy>=1.12
# scikit-learn>=1.4

# Optional: TensorFlow/PyTorch – not needed for Resq API (detectors run via subprocess).
# If you run the detector scripts, use Python 3.11/3.12 and:
# pip install tensorflow opencv-python
# pip install torch torchvision
